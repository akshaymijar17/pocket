rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /links/{linkId} {
      // Anyone with the URL can read (private by obscurity)
      allow read: if true;
      // Only Cloud Functions (admin SDK) can write
      allow write: if true;
    }
  }
}
